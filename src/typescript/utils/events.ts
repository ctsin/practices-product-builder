import { when } from "../helper";

export const m75Selected = when("m75Selected");
export const modelSelected = when("modelSelected");
export const prevClicked = when("prevClicked");
export const nextClicked = when("nextClicked");
export const activeUpdated = when("activeUpdated");
